services:
  - type: web
    name: vismartcarhire
    env: static
    buildCommand: cd frontend && npm ci && NODE_OPTIONS='--max-old-space-size=4096' npm run build:prod
    staticPublishPath: ./frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html